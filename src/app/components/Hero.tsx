"use client";
import React from "react";

const Hero = () => {
  return (
    <section>
      {/* Hero sekce: headline, podtitul, CTA, scroll-indicator */}
    </section>
  );
};

export default Hero; 